apiVersion: apps/v1
kind: Deployment
metadata:
  name: predictive-analytics
  labels:
    app: predictive-analytics
    project: smartbankAI
spec:
  replicas: 2
  selector:
    matchLabels:
      app: predictive-analytics
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: predictive-analytics
        version: "1.0.0"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: predictive-analytics
          image: ghcr.io/infinityai-africa/smartbankAI-predictive-analytics:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8006
              protocol: TCP
          envFrom:
            - configMapRef:
                name: smartbank-config
            - secretRef:
                name: smartbank-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: models
              mountPath: /app/models
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: models-pvc-predictive-analytics
      restartPolicy: Always
